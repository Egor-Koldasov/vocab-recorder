import { State } from "./types/Store";

export const initialState: State = {
  cursor: null,
  cursorPoint: null,
  openBox: null,
  contextMenuWord: null,
  sourceLanguage: 'pt',
  targetLanguage: 'en',
};
